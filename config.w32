// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("predis", "for predis support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("predis", "enable predis support", "no");

if (PHP_PREDIS != "no") {
	EXTENSION("predis", "predis.c");
}

